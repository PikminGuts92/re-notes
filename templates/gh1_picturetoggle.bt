#include "milo_common.bt"
#include "gh1_common.bt"

LittleEndian();
// (None)
// Super: Trans Draw
// Ext: .tog

struct PictureToggle
{
    int version; // Always 0
    Trans trans;
    Draw draw;
    
    NumString element; // Element to be toggled (Usually a checkbox)
} file;