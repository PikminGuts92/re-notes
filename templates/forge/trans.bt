#ifndef FORGE_TRANS
#define FORGE_TRANS

#include "common.bt"
#include "object.bt"

typedef struct (int super)
{
    int revision; // 10 (DCS/FME)
    if (!super) Object object;

    Matrix localXfm;
    Matrix worldXfm;
    
    byte empty[9];
    FString parent;
} Trans;

#endif