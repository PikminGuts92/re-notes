// Abstract base class for pollable RND objects
// Super: (None)
// Ext: (None)

#ifndef OBJECT_POLL
#define OBJECT_POLL

#include "common.bt"

typedef struct (SystemInfo& info, Bool super)
{
    NumString str1; // null.env
    NumString str2; // start
} Poll;

#endif