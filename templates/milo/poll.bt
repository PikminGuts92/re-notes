// Abstract base class for pollable RND objects
// Super: (None)
// Ext: (None)

#ifndef OBJECT_POLL
#define OBJECT_POLL

#include "common.bt"

typedef struct (SystemInfo& info, Bool super)
{
    // Not sure of field names
    NumString exit;  // null.env
    NumString enter; // start
} Poll;

#endif