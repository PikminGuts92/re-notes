// Shared instance of a RndDir
// Super: RndDir
// Ext: inst

#ifndef WORLD_INSTANCE
#define WORLD_INSTANCE

#include "common.bt"
#include "rnd_dir.bt"

typedef struct (SystemInfo& info, Bool super)
{
    int version; // 1 (TBRB), 2 (DC1)
    NumString unk_milo;

    RndDir rnd_dir(info, True);
} WorldInstance;

#endif